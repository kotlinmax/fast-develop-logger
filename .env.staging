HTTP_SERVER_PORT                 = 4000

APP_ENV                          = staging
APP_NAME                         = stage-logger

POSTGRES_SERVICE                 = postgres
POSTGRES_PASSWORD                = postgres
POSTGRES_USER                    = postgres
POSTGRES_DB                      = stage-logger-db
POSTGRES_PORT                    = 5432
POSTGRES_MAX                     = 200
POSTGRES_SSL                     = false

LOGGER_CONSUMER_BROKER           = localhost:9092
LOGGER_CONSUMER_GROUP_ID         = stage-logger-group
LOGGER_CONSUMER_CLIENT_ID        = stage-logger-client
LOGGER_CONSUMER_TOPIC            = stage-logger-topic

BATCH_SIZE_LOG_RECORD            = 100

DATABASE_URL                     = postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_SERVICE}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public
